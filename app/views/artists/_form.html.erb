<%= render partial: 'shared/errors', locals: {target: @artist} %>
<%= form_for(@artist) do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :image_path %>
<%= f.text_field :image_path %>

<%= f.submit %>
<% end %>
